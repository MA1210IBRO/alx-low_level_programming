#include "main.h"
/**
 * _isupper - check upper case
 * @c: the number to be cheked
 * Return: give 1 or 0
 */
int _isupper(int c)
{
	if (if c >= 'A' && c <= 'Z')
	{
		return (1);
	}

	else
	{
		return (0);
	}
}
